# Rust Mathematics

```
cargo build --release 
cargo test
cargo clippy

cargo run --release --bin maths         # Hello World!
cargo run --release --bin subsets
cargo run --release --bin riemann
cargo run --release --bin fermat_rug    > src/bin/fermat_rug.txt
cargo run --release --bin fermat_bigint > src/bin/fermat_bigint.txt
```
